//- 如果启用了 Google AdSense 功能
if (theme.google_adsense && theme.google_adsense.enable)
  //- 异步加载 Google AdSense 的 JavaScript 文件
  script(async src=theme.google_adsense.js)

  //- 如果启用了自动广告
  if theme.google_adsense.auto_ads
    //- 初始化并推送 Google AdSense 配置
    script.
      (adsbygoogle = window.adsbygoogle || []).push({
        google_ad_client: '!{theme.google_adsense.client}', //- Google AdSense 客户端 ID
        enable_page_level_ads: '!{theme.google_adsense.enable_page_level_ads}' //- 是否启用页面级广告
      });


//- 代码功能解释：
//- 检查 Google AdSense 功能：判断主题配置中是否启用了 Google AdSense。
//- 加载 AdSense 脚本：如果启用，异步加载 AdSense 的 JavaScript 文件。
//- 自动广告设置：如果启用了自动广告，则初始化 adsbygoogle 数组并推送相关广告配置，包括客户端 ID 和页面级广告的启用状态。